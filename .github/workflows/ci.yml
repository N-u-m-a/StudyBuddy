name: StudyBuddy CI/CD Pipeline

on:
  push:
    branches:
      - development

  pull_request:
    branches:
      - development

jobs:
  ci:
    name: CI
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check project structure
        run: |
          echo "StudyBuddy CI pipeline is running"
          echo "Repository checked out successfully"
          echo "Backend and frontend Docker builds will be added when application code is available"

      - name: Install Newman
        run: npm install -g newman

      - name: Run Postman API Tests
        run: newman run "postman/jsonplaceholder API Tests.postman_collection.json"